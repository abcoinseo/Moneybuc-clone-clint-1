<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EarnFlux Admin Panel</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #3b82f6;
            --primary-dark: #2563eb;
            --primary-light: #60a5fa;
            --accent-blue: #0ea5e9;
            --background-dark: #0a0a0a;
            --surface-dark: #18181b;
            --surface-dark-alt: #27272a;
            --border-color: #3f3f46;
            --text-primary: #ffffff;
            --text-secondary: #d4d4d8;
            --text-muted: #71717a;
            --success: #10b981;
            --danger: #dc2626;
            --warning: #f59e0b;
            --info: #0284c7;
            --glass-backdrop-filter: blur(25px);
            --glass-overlay-dark: rgba(24, 24, 27, 0.7);
            --glass-border: rgba(63, 63, 70, 0.5);
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--background-dark);
            color: var(--text-primary);
            overflow-x: hidden;
            position: relative;
            line-height: 1.6;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background:
                radial-gradient(circle at 20% 80%, rgba(59, 130, 246, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(96, 165, 250, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(37, 99, 235, 0.1) 0%, transparent 50%);
            pointer-events: none;
            z-index: -1;
            animation: subtleGradients 15s ease infinite alternate;
        }

        @keyframes subtleGradients {
            0% { opacity: 0.8; }
            50% { opacity: 1; }
            100% { opacity: 0.8; }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: var(--glass-overlay-dark);
            backdrop-filter: var(--glass-backdrop-filter);
            border: 1px solid var(--glass-border);
            border-radius: 24px;
            padding: 25px 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.4);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header h1 {
            font-size: 2rem;
            font-weight: 800;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .nav-tabs {
            display: flex;
            background: var(--surface-dark);
            border-radius: 12px;
            padding: 5px;
            margin-bottom: 25px;
            border: 1px solid var(--border-color);
            overflow-x: auto;
        }

        .nav-tab {
            padding: 12px 20px;
            text-align: center;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 600;
            color: var(--text-muted);
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .nav-tab.active {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            box-shadow: 0 5px 15px rgba(59, 130, 246, 0.3);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .card {
            background: var(--surface-dark);
            backdrop-filter: var(--glass-backdrop-filter);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--primary), var(--primary-light), var(--primary));
            opacity: 0.8;
        }

        .card-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--text-primary);
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: var(--surface-dark-alt);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 25px;
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }

        .stat-number {
            font-size: 2.2rem;
            font-weight: 900;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .stat-label {
            color: var(--text-secondary);
            font-weight: 500;
            font-size: 0.9rem;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
            text-decoration: none;
            font-size: 0.9rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: var(--text-primary);
        }

        .btn-success {
            background: linear-gradient(135deg, var(--success), #047857);
            color: var(--text-primary);
        }

        .btn-warning {
            background: linear-gradient(135deg, var(--warning), #b45309);
            color: var(--text-primary);
        }

        .btn-danger {
            background: linear-gradient(135deg, var(--danger), #b91c1c);
            color: var(--text-primary);
        }

        .btn-secondary {
            background: var(--surface-dark-alt);
            color: var(--text-secondary);
            border: 1px solid var(--border-color);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-secondary);
        }

        .form-input, .form-select, .form-textarea {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid var(--border-color);
            border-radius: 10px;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            background: var(--surface-dark-alt);
            color: var(--text-primary);
            outline: none;
        }

        .form-input:focus, .form-select:focus, .form-textarea:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.15);
        }

        .form-textarea {
            resize: vertical;
            min-height: 100px;
        }

        .table-container {
            overflow-x: auto;
            border-radius: 12px;
            border: 1px solid var(--border-color);
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            background: var(--surface-dark-alt);
        }

        .table th,
        .table td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
        }

        .table th {
            background: var(--surface-dark);
            color: var(--text-primary);
            font-weight: 600;
            position: sticky;
            top: 0;
        }

        .table tr:hover {
            background: rgba(59, 130, 246, 0.05);
        }

        .status-badge {
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .status-active { background: rgba(16, 185, 121, 0.2); color: var(--success); }
        .status-banned { background: rgba(220, 38, 38, 0.2); color: var(--danger); }
        .status-pending { background: rgba(217, 119, 6, 0.2); color: var(--warning); }
        .status-completed { background: rgba(16, 185, 121, 0.2); color: var(--success); }
        .status-rejected { background: rgba(220, 38, 38, 0.2); color: var(--danger); }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.85);
            z-index: 2000;
            backdrop-filter: blur(15px);
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .modal-content {
            background: var(--surface-dark);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 30px;
            max-width: 600px;
            width: 100%;
            max-height: 80vh;
            overflow-y: auto;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .modal-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary);
        }

        .close-btn {
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: var(--text-muted);
            transition: all 0.3s ease;
        }

        .close-btn:hover {
            color: var(--text-secondary);
            transform: scale(1.1);
        }

        .alert {
            padding: 15px 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            border-left: 4px solid;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .alert-success { background: rgba(16, 185, 121, 0.1); border-left-color: var(--success); color: var(--success); }
        .alert-danger { background: rgba(220, 38, 38, 0.1); border-left-color: var(--danger); color: var(--danger); }
        .alert-warning { background: rgba(217, 119, 6, 0.1); border-left-color: var(--warning); color: var(--warning); }
        .alert-info { background: rgba(2, 132, 199, 0.1); border-left-color: var(--info); color: var(--info); }

        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, .3);
            border-radius: 50%;
            border-top-color: var(--text-primary);
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .search-box {
            margin-bottom: 20px;
        }

        .search-box input {
            width: 100%;
            max-width: 400px;
            padding: 12px 40px 12px 16px;
            border: 2px solid var(--border-color);
            border-radius: 25px;
            background: var(--surface-dark-alt);
            color: var(--text-primary);
            font-size: 0.9rem;
        }

        .action-buttons {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .grid-2 {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .grid-3 {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 20px;
        }

        @media (max-width: 768px) {
            .grid-2, .grid-3 {
                grid-template-columns: 1fr;
            }
            
            .nav-tabs {
                overflow-x: auto;
            }
            
            .action-buttons {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>
                <span class="material-icons">admin_panel_settings</span>
                EarnFlux Admin Panel
            </h1>
            <button class="btn btn-primary" onclick="refreshData()">
                <span class="material-icons">refresh</span>
                Refresh All Data
            </button>
        </div>

        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number" id="totalUsers">0</div>
                <div class="stat-label">Total Users</div>
            </div>
            <div class="stat-card">
                <div class="stat-number" id="activeTasks">0</div>
                <div class="stat-label">Active Tasks</div>
            </div>
            <div class="stat-card">
                <div class="stat-number" id="pendingWithdrawals">0</div>
                <div class="stat-label">Pending Withdrawals</div>
            </div>
            <div class="stat-card">
                <div class="stat-number" id="totalEarningsDistributed">0</div>
                <div class="stat-label">Total Earnings (Points)</div>
            </div>
            <div class="stat-card">
                <div class="stat-number" id="activeLuckyCodes">0</div>
                <div class="stat-label">Active Lucky Codes</div>
            </div>
        </div>

        <div class="nav-tabs">
            <div class="nav-tab active" onclick="showTab('users')">
                <span class="material-icons">people</span>
                Users Management
            </div>
            <div class="nav-tab" onclick="showTab('tasks')">
                <span class="material-icons">assignment</span>
                Tasks Management
            </div>
            <div class="nav-tab" onclick="showTab('withdrawals')">
                <span class="material-icons">account_balance_wallet</span>
                Withdrawals
            </div>
            <div class="nav-tab" onclick="showTab('codes')">
                <span class="material-icons">card_giftcard</span>
                Lucky Codes
            </div>
            <div class="nav-tab" onclick="showTab('settings')">
                <span class="material-icons">settings</span>
                System Settings
            </div>
            <div class="nav-tab" onclick="showTab('analytics')">
                <span class="material-icons">analytics</span>
                Analytics
            </div>
        </div>

        <!-- Users Management Tab -->
        <div id="usersTab" class="tab-content active">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">people</span>
                    Users Management
                </div>
                
                <div class="search-box">
                    <input type="text" id="userSearch" placeholder="Search users by ID, username, or IP..." onkeyup="filterUsers()">
                </div>

                <div class="action-buttons" style="margin-bottom: 20px;">
                    <button class="btn btn-primary" onclick="showModal('addBalanceModal')">
                        <span class="material-icons">add</span>
                        Add Balance
                    </button>
                    <button class="btn btn-warning" onclick="showModal('changeBalanceModal')">
                        <span class="material-icons">edit</span>
                        Change Balance
                    </button>
                    <button class="btn btn-danger" onclick="bulkBanByIP()">
                        <span class="material-icons">block</span>
                        Bulk Ban by IP
                    </button>
                </div>

                <div class="table-container">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>User ID</th>
                                <th>Username</th>
                                <th>First Name</th>
                                <th>Balance</th>
                                <th>TON Balance</th>
                                <th>Total Earned</th>
                                <th>Tasks Completed</th>
                                <th>IP Address</th>
                                <th>Status</th>
                                <th>Joined</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="usersTableBody">
                            <!-- Users will be loaded here -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Tasks Management Tab -->
        <div id="tasksTab" class="tab-content">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">assignment</span>
                    Tasks Management
                </div>

                <div class="action-buttons" style="margin-bottom: 20px;">
                    <button class="btn btn-primary" onclick="showModal('addTaskModal')">
                        <span class="material-icons">add</span>
                        Add Task
                    </button>
                </div>

                <div class="table-container">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Task ID</th>
                                <th>Title</th>
                                <th>Category</th>
                                <th>URL</th>
                                <th>Reward</th>
                                <th>Impressions</th>
                                <th>Remaining</th>
                                <th>Created By</th>
                                <th>Transaction</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="tasksTableBody">
                            <!-- Tasks will be loaded here -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Withdrawals Tab -->
        <div id="withdrawalsTab" class="tab-content">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">account_balance_wallet</span>
                    Withdrawal Requests
                </div>

                <div class="table-container">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Request ID</th>
                                <th>User ID</th>
                                <th>Username</th>
                                <th>TON Address</th>
                                <th>Amount</th>
                                <th>Status</th>
                                <th>Created</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="withdrawalsTableBody">
                            <!-- Withdrawals will be loaded here -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- Lucky Codes Tab -->
        <div id="codesTab" class="tab-content">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">card_giftcard</span>
                    Lucky Codes Management
                </div>

                <div class="action-buttons" style="margin-bottom: 20px;">
                    <button class="btn btn-primary" onclick="showModal('addCodeModal')">
                        <span class="material-icons">add</span>
                        Create Lucky Code
                    </button>
                </div>

                <div class="table-container">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Code ID</th>
                                <th>Code</th>
                                <th>Reward</th>
                                <th>Used</th>
                                <th>Expires</th>
                                <th>Created</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="codesTableBody">
                            <!-- Lucky codes will be loaded here -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <!-- System Settings Tab -->
        <div id="settingsTab" class="tab-content">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">settings</span>
                    System Settings
                </div>

                <div class="grid-2">
                    <div class="card">
                        <h3>Game Settings</h3>
                        <div class="form-group">
                            <label class="form-label">Daily Ad Limit</label>
                            <input type="number" id="maxDailyAds" class="form-input" value="50" min="1" max="200">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Ad Reward Points</label>
                            <input type="number" id="adRewardPoints" class="form-input" value="15" min="1" max="100">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Daily Checkin Reward</label>
                            <input type="number" id="checkinReward" class="form-input" value="20" min="1" max="200">
                        </div>
                        <button class="btn btn-success" onclick="updateGameSettings()">Update Settings</button>
                    </div>

                    <div class="card">
                        <h3>Exchange Rates</h3>
                        <div class="form-group">
                            <label class="form-label">Points to TON Rate</label>
                            <input type="number" id="pointsToTonRate" class="form-input" value="0.00001" step="0.00000001">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Impressions to TON Rate</label>
                            <input type="number" id="impressionsToTonRate" class="form-input" value="0.0015" step="0.0001">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Referral Bonus Rate (%)</label>
                            <input type="number" id="referralRate" class="form-input" value="20" min="1" max="50">
                        </div>
                        <button class="btn btn-success" onclick="updateExchangeRates()">Update Rates</button>
                    </div>
                </div>

                <div class="card">
                    <h3>Database Actions</h3>
                    <div class="action-buttons">
                        <button class="btn btn-danger" onclick="confirmAction('clearAllTasks', 'delete all tasks')">
                            <span class="material-icons">delete_sweep</span>
                            Clear All Tasks
                        </button>
                        <button class="btn btn-danger" onclick="confirmAction('clearAllWithdrawals', 'clear all withdrawals')">
                            <span class="material-icons">clear_all</span>
                            Clear Withdrawals
                        </button>
                        <button class="btn btn-warning" onclick="resetAllUserBalances()">
                            <span class="material-icons">restart_alt</span>
                            Reset All Balances
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Analytics Tab -->
        <div id="analyticsTab" class="tab-content">
            <div class="card">
                <div class="card-title">
                    <span class="material-icons">analytics</span>
                    System Analytics
                </div>

                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsNewUsersToday">0</div>
                        <div class="stat-label">New Users Today</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsTasksCreatedToday">0</div>
                        <div class="stat-label">Tasks Created Today</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsWithdrawalsToday">0</div>
                        <div class="stat-label">Withdrawals Today</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsBannedUsers">0</div>
                        <div class="stat-label">Banned Users</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsActiveTasksTotal">0</div>
                        <div class="stat-label">Active Tasks Total</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="analyticsTotalTonWithdrawn">0.00</div>
                        <div class="stat-label">Total TON Withdrawn</div>
                    </div>
                </div>

                <div class="card">
                    <h3>IP Address Analysis</h3>
                    <div class="table-container">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>IP Address</th>
                                    <th>Users Count</th>
                                    <th>Total Earnings</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody id="ipAnalysisTableBody">
                                <!-- IP analysis will be loaded here -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modals -->

    <!-- Add Balance Modal -->
    <div id="addBalanceModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Add Balance to User</h3>
                <button class="close-btn" onclick="closeModal('addBalanceModal')">&times;</button>
            </div>
            <form id="addBalanceForm">
                <div class="form-group">
                    <label class="form-label">User ID</label>
                    <input type="text" id="addBalanceUserId" class="form-input" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Points to Add</label>
                    <input type="number" id="addBalanceAmount" class="form-input" min="1" required>
                </div>
                <button type="submit" class="btn btn-success">Add Balance</button>
            </form>
        </div>
    </div>

    <!-- Change Balance Modal -->
    <div id="changeBalanceModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Change User Balance</h3>
                <button class="close-btn" onclick="closeModal('changeBalanceModal')">&times;</button>
            </div>
            <form id="changeBalanceForm">
                <div class="form-group">
                    <label class="form-label">User ID</label>
                    <input type="text" id="changeBalanceUserId" class="form-input" required>
                </div>
                <div class="form-group">
                    <label class="form-label">New Balance</label>
                    <input type="number" id="changeBalanceAmount" class="form-input" min="0" required>
                </div>
                <div class="form-group">
                    <label class="form-label">New TON Balance</label>
                    <input type="number" id="changeTonBalanceAmount" class="form-input" min="0" step="0.00000001" required>
                </div>
                <button type="submit" class="btn btn-warning">Change Balance</button>
            </form>
        </div>
    </div>

    <!-- Add Task Modal -->
    <div id="addTaskModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Create New Task</h3>
                <button class="close-btn" onclick="closeModal('addTaskModal')">&times;</button>
            </div>
            <form id="addTaskForm">
                <div class="form-group">
                    <label class="form-label">Task Title</label>
                    <input type="text" id="taskTitle" class="form-input" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Description</label>
                    <textarea id="taskDescription" class="form-textarea" required></textarea>
                </div>
                <div class="form-group">
                    <label class="form-label">Task URL</label>
                    <input type="url" id="taskUrl" class="form-input" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Category</label>
                    <select id="taskCategory" class="form-select" required>
                        <option value="normal">Normal Task</option>
                        <option value="telegram">Telegram Task</option>
                    </select>
                </div>
                <div class="form-group">
                    <label class="form-label">Telegram Channel (for Telegram tasks)</label>
                    <input type="text" id="taskTelegramChannel" class="form-input" placeholder="channelname">
                </div>
                <div class="form-group">
                    <label class="form-label">Reward Points</label>
                    <input type="number" id="taskReward" class="form-input" value="100" min="1" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Impressions</label>
                    <input type="number" id="taskImpressions" class="form-input" value="1000" min="100" required>
                </div>
                <button type="submit" class="btn btn-primary">Create Task</button>
            </form>
        </div>
    </div>

    <!-- Add Lucky Code Modal -->
    <div id="addCodeModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Create Lucky Code</h3>
                <button class="close-btn" onclick="closeModal('addCodeModal')">&times;</button>
            </div>
            <form id="addCodeForm">
                <div class="form-group">
                    <label class="form-label">Code</label>
                    <input type="text" id="luckyCode" class="form-input" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Reward Points</label>
                    <input type="number" id="luckyCodeReward" class="form-input" value="50" min="1" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Expires In (hours)</label>
                    <input type="number" id="luckyCodeExpiry" class="form-input" value="24" min="1" required>
                </div>
                <button type="submit" class="btn btn-primary">Create Code</button>
            </form>
        </div>
    </div>

    <!-- Firebase -->
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-database-compat.js"></script>

    <script>
        // Firebase Configuration
        const firebaseConfig = {
            apiKey: "AIzaSyA3SqCHzN3kLj7DC8Cyu9OzxPFtIlyyii4",
            authDomain: "maneybux-clone.firebaseapp.com",
            projectId: "maneybux-clone",
            storageBucket: "maneybux-clone.firebasestorage.app",
            messagingSenderId: "153207109623",
            appId: "1:153207109623:web:4cd48633c49c2ba3c1a0f2"
        };

        try {
            if (!firebase.apps.length) {
                firebase.initializeApp(firebaseConfig);
            }
            var database = firebase.database();
        } catch (error) {
            console.error("Firebase initialization error:", error);
            showAlert("Firebase initialization failed. Please check your connection.", "danger");
        }

        let allUsers = {};
        let allTasks = {};
        let allWithdrawals = {};
        let allCodes = {};

        // Initialize Admin Panel
        document.addEventListener('DOMContentLoaded', () => {
            loadAllData();
        });

        // Tab Management
        function showTab(tabName) {
            document.querySelectorAll('.nav-tab').forEach(tab => {
                tab.classList.remove('active');
            });
            document.querySelectorAll('.tab-content').forEach(content => {
                content.classList.remove('active');
            });

            event.target.classList.add('active');
            document.getElementById(tabName + 'Tab').classList.add('active');

            if (tabName === 'analytics') {
                loadAnalytics();
            }
        }

        // Load All Data
        async function loadAllData() {
            showAlert("Loading all data...", "info");
            
            try {
                await Promise.all([
                    loadUsers(),
                    loadTasks(),
                    loadWithdrawals(),
                    loadLuckyCodes(),
                    updateStats()
                ]);
                
                showAlert("All data loaded successfully!", "success");
            } catch (error) {
                console.error("Error loading data:", error);
                showAlert("Failed to load some data. Please refresh.", "danger");
            }
        }

        // Load Users
        function loadUsers() {
            return new Promise((resolve, reject) => {
                database.ref('users').on('value', (snapshot) => {
                    allUsers = snapshot.val() || {};
                    updateUsersTable();
                    resolve();
                }, reject);
            });
        }

        // Load Tasks
        function loadTasks() {
            return new Promise((resolve, reject) => {
                database.ref('tasks').on('value', (snapshot) => {
                    allTasks = snapshot.val() || {};
                    updateTasksTable();
                    resolve();
                }, reject);
            });
        }

        // Load Withdrawals
        function loadWithdrawals() {
            return new Promise((resolve, reject) => {
                database.ref('withdrawals').on('value', (snapshot) => {
                    allWithdrawals = snapshot.val() || {};
                    updateWithdrawalsTable();
                    resolve();
                }, reject);
            });
        }

        // Load Lucky Codes
        function loadLuckyCodes() {
            return new Promise((resolve, reject) => {
                database.ref('luckyCodes').on('value', (snapshot) => {
                    allCodes = snapshot.val() || {};
                    updateCodesTable();
                    resolve();
                }, reject);
            });
        }

        // Update Users Table
        function updateUsersTable() {
            const tbody = document.getElementById('usersTableBody');
            tbody.innerHTML = '';

            Object.keys(allUsers).forEach(userId => {
                const user = allUsers[userId];
                const row = document.createElement('tr');
                
                row.innerHTML = `
                    <td>${userId}</td>
                    <td>${user.username || 'N/A'}</td>
                    <td>${user.first_name || 'N/A'}</td>
                    <td>${formatNumber(user.balance || 0)}</td>
                    <td>${(user.tonBalance || 0).toFixed(8)}</td>
                    <td>${formatNumber(user.totalEarned || 0)}</td>
                    <td>${user.tasksCompleted || 0}</td>
                    <td>${user.ipAddress || 'N/A'}</td>
                    <td><span class="status-badge status-${user.status || 'active'}">${(user.status || 'active').toUpperCase()}</span></td>
                    <td>${user.createdAt ? new Date(user.createdAt).toLocaleDateString() : 'N/A'}</td>
                    <td>
                        <div class="action-buttons">
                            ${user.status === 'banned' ? 
                                `<button class="btn btn-success" onclick="toggleUserStatus('${userId}', 'active')">
                                    <span class="material-icons">person</span> Unban
                                </button>` :
                                `<button class="btn btn-danger" onclick="toggleUserStatus('${userId}', 'banned')">
                                    <span class="material-icons">block</span> Ban
                                </button>`
                            }
                            <button class="btn btn-secondary" onclick="editUser('${userId}')">
                                <span class="material-icons">edit</span>
                            </button>
                        </div>
                    </td>
                `;
                tbody.appendChild(row);
            });
        }

        // Update Tasks Table
        function updateTasksTable() {
            const tbody = document.getElementById('tasksTableBody');
            tbody.innerHTML = '';

            Object.keys(allTasks).forEach(taskId => {
                const task = allTasks[taskId];
                const row = document.createElement('tr');
                
                const creatorInfo = allUsers[task.createdBy] ? 
                    `${allUsers[task.createdBy].first_name} (${task.createdBy})` : 
                    task.createdBy || 'System';

                row.innerHTML = `
                    <td>${taskId.slice(-8)}</td>
                    <td>${task.title || 'N/A'}</td>
                    <td><span class="status-badge status-${task.category === 'telegram' ? 'pending' : 'active'}">${task.category || 'normal'}</span></td>
                    <td><a href="${task.url}" target="_blank" style="color: var(--primary);">${task.url ? task.url.slice(0, 30) + '...' : 'N/A'}</a></td>
                    <td>${task.reward || 100}</td>
                    <td>${task.impressions || 0}</td>
                    <td>${task.remainingImpressions || task.impressions || 0}</td>
                    <td>${creatorInfo}</td>
                    <td>${task.transactionHash ? task.transactionHash.slice(0, 8) + '...' : 'N/A'}</td>
                    <td><span class="status-badge status-${task.status || 'active'}">${(task.status || 'active').toUpperCase()}</span></td>
                    <td>
                        <div class="action-buttons">
                            <button class="btn btn-secondary" onclick="editTask('${taskId}')">
                                <span class="material-icons">edit</span>
                            </button>
                            <button class="btn btn-danger" onclick="deleteTask('${taskId}')">
                                <span class="material-icons">delete</span>
                            </button>
                        </div>
                    </td>
                `;
                tbody.appendChild(row);
            });
        }

        // Update Withdrawals Table
        function updateWithdrawalsTable() {
            const tbody = document.getElementById('withdrawalsTableBody');
            tbody.innerHTML = '';

            Object.keys(allWithdrawals).forEach(withdrawalId => {
                const withdrawal = allWithdrawals[withdrawalId];
                const row = document.createElement('tr');
                
                row.innerHTML = `
                    <td>${withdrawalId.slice(-8)}</td>
                    <td>${withdrawal.userId}</td>
                    <td>${withdrawal.username || 'N/A'}</td>
                    <td style="font-family: monospace;">${withdrawal.tonAddress ? withdrawal.tonAddress.slice(0, 10) + '...' + withdrawal.tonAddress.slice(-10) : 'N/A'}</td>
                    <td>${(withdrawal.amountTon || 0).toFixed(8)} TON</td>
                    <td><span class="status-badge status-${withdrawal.status || 'pending'}">${(withdrawal.status || 'pending').toUpperCase()}</span></td>
                    <td>${withdrawal.createdAt ? new Date(withdrawal.createdAt).toLocaleDateString() : 'N/A'}</td>
                    <td>
                        <div class="action-buttons">
                            ${withdrawal.status === 'pending' ? 
                                `<button class="btn btn-success" onclick="approveWithdrawal('${withdrawalId}')">
                                    <span class="material-icons">check_circle</span> Approve
                                </button>
                                <button class="btn btn-danger" onclick="rejectWithdrawal('${withdrawalId}')">
                                    <span class="material-icons">cancel</span> Reject
                                </button>` :
                                `<button class="btn btn-secondary" onclick="viewWithdrawal('${withdrawalId}')">
                                    <span class="material-icons">visibility</span> View
                                </button>`
                            }
                        </div>
                    </td>
                `;
                tbody.appendChild(row);
            });
        }

        // Update Codes Table
        function updateCodesTable() {
            const tbody = document.getElementById('codesTableBody');
            tbody.innerHTML = '';

            Object.keys(allCodes).forEach(codeId => {
                const code = allCodes[codeId];
                const row = document.createElement('tr');
                
                const isExpired = code.expiresAt < Date.now();
                const expiryDate = new Date(code.expiresAt).toLocaleDateString();
                const createdDate = code.createdAt ? new Date(code.createdAt).toLocaleDateString() : 'N/A';
                
                row.innerHTML = `
                    <td>${codeId.slice(-8)}</td>
                    <td style="font-family: monospace; font-weight: bold; color: var(--primary);">${code.code}</td>
                    <td>${code.reward || 0} points</td>
                    <td><span class="status-badge status-${code.used ? 'completed' : 'active'}">${code.used ? 'USED' : 'ACTIVE'}</span></td>
                    <td style="color: ${isExpired ? 'var(--danger)' : 'var(--success)'};">${expiryDate}</td>
                    <td>${createdDate}</td>
                    <td>
                        <div class="action-buttons">
                            <button class="btn btn-danger" onclick="deleteCode('${codeId}')">
                                <span class="material-icons">delete</span>
                            </button>
                        </div>
                    </td>
                `;
                tbody.appendChild(row);
            });
        }

        // Update Statistics
        function updateStats() {
            const totalUsers = Object.keys(allUsers).length;
            const activeTasks = Object.values(allTasks).filter(task => task.status === 'active').length;
            const pendingWithdrawals = Object.values(allWithdrawals).filter(w => w.status === 'pending').length;
            const activeCodes = Object.values(allCodes).filter(code => !code.used && code.expiresAt > Date.now()).length;
            
            const totalEarningsDistributed = Object.values(allUsers).reduce((sum, user) => 
                sum + (user.totalEarned || 0), 0);

            document.getElementById('totalUsers').textContent = formatNumber(totalUsers);
            document.getElementById('activeTasks').textContent = formatNumber(activeTasks);
            document.getElementById('pendingWithdrawals').textContent = formatNumber(pendingWithdrawals);
            document.getElementById('totalEarningsDistributed').textContent = formatNumber(totalEarningsDistributed);
            document.getElementById('activeLuckyCodes').textContent = formatNumber(activeCodes);
        }

        // User Management Functions
        async function toggleUserStatus(userId, newStatus) {
            if (confirm(`Are you sure you want to ${newStatus === 'banned' ? 'ban' : 'unban'} this user?`)) {
                try {
                    await database.ref('users/' + userId).update({ status: newStatus });
                    showAlert(`User ${newStatus === 'banned' ? 'banned' : 'unbanned'} successfully!`, 'success');
                } catch (error) {
                    console.error('Error updating user status:', error);
                    showAlert('Failed to update user status.', 'danger');
                }
            }
        }

        function editUser(userId) {
            const user = allUsers[userId];
            document.getElementById('changeBalanceUserId').value = userId;
            document.getElementById('changeBalanceAmount').value = user.balance || 0;
            document.getElementById('changeTonBalanceAmount').value = user.tonBalance || 0;
            showModal('changeBalanceModal');
        }

        async function bulkBanByIP() {
            const ipAddress = prompt('Enter IP address to ban all users from:');
            if (!ipAddress) return;

            const usersToban = Object.keys(allUsers).filter(userId => 
                allUsers[userId].ipAddress === ipAddress && allUsers[userId].status !== 'banned'
            );

            if (usersToban.length === 0) {
                showAlert('No active users found with this IP address.', 'warning');
                return;
            }

            if (confirm(`This will ban ${usersToban.length} users with IP ${ipAddress}. Continue?`)) {
                try {
                    const updates = {};
                    usersToban.forEach(userId => {
                        updates[`users/${userId}/status`] = 'banned';
                    });
                    
                    await database.ref().update(updates);
                    showAlert(`Successfully banned ${usersToban.length} users with IP ${ipAddress}!`, 'success');
                } catch (error) {
                    console.error('Error bulk banning users:', error);
                    showAlert('Failed to ban users. Please try again.', 'danger');
                }
            }
        }

        // Task Management Functions
        async function deleteTask(taskId) {
            if (confirm('Are you sure you want to delete this task? This action cannot be undone.')) {
                try {
                    await database.ref('tasks/' + taskId).remove();
                    showAlert('Task deleted successfully!', 'success');
                } catch (error) {
                    console.error('Error deleting task:', error);
                    showAlert('Failed to delete task.', 'danger');
                }
            }
        }

        function editTask(taskId) {
            const task = allTasks[taskId];
            
            // Pre-fill the form with current task data
            document.getElementById('taskTitle').value = task.title || '';
            document.getElementById('taskDescription').value = task.description || '';
            document.getElementById('taskUrl').value = task.url || '';
            document.getElementById('taskCategory').value = task.category || 'normal';
            document.getElementById('taskTelegramChannel').value = task.telegramChannel || '';
            document.getElementById('taskReward').value = task.reward || 100;
            document.getElementById('taskImpressions').value = task.impressions || 1000;
            
            // Store the task ID for updating
            document.getElementById('addTaskForm').dataset.editingTaskId = taskId;
            document.querySelector('#addTaskModal .modal-title').textContent = 'Edit Task';
            document.querySelector('#addTaskForm button[type="submit"]').innerHTML = '<span class="material-icons">save</span> Update Task';
            
            showModal('addTaskModal');
        }

        // Withdrawal Management Functions
        async function approveWithdrawal(withdrawalId) {
            if (confirm('Are you sure you want to approve this withdrawal?')) {
                try {
                    await database.ref('withdrawals/' + withdrawalId).update({ 
                        status: 'completed',
                        processedAt: Date.now()
                    });
                    showAlert('Withdrawal approved successfully!', 'success');
                } catch (error) {
                    console.error('Error approving withdrawal:', error);
                    showAlert('Failed to approve withdrawal.', 'danger');
                }
            }
        }

        async function rejectWithdrawal(withdrawalId) {
            const reason = prompt('Enter rejection reason (optional):');
            
            try {
                const withdrawal = allWithdrawals[withdrawalId];
                
                // Return TON to user's balance
                await database.ref('users/' + withdrawal.userId).transaction((userData) => {
                    if (userData) {
                        userData.tonBalance = (userData.tonBalance || 0) + withdrawal.amountTon;
                    }
                    return userData;
                });

                // Update withdrawal status
                await database.ref('withdrawals/' + withdrawalId).update({ 
                    status: 'rejected',
                    rejectionReason: reason || 'No reason provided',
                    processedAt: Date.now()
                });
                
                showAlert('Withdrawal rejected and TON returned to user!', 'success');
            } catch (error) {
                console.error('Error rejecting withdrawal:', error);
                showAlert('Failed to reject withdrawal.', 'danger');
            }
        }

        // Lucky Code Management
        async function deleteCode(codeId) {
            if (confirm('Are you sure you want to delete this lucky code?')) {
                try {
                    await database.ref('luckyCodes/' + codeId).remove();
                    showAlert('Lucky code deleted successfully!', 'success');
                } catch (error) {
                    console.error('Error deleting code:', error);
                    showAlert('Failed to delete code.', 'danger');
                }
            }
        }

        // Form Handlers
        document.getElementById('addBalanceForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const userId = document.getElementById('addBalanceUserId').value;
            const amount = parseInt(document.getElementById('addBalanceAmount').value);
            
            if (!allUsers[userId]) {
                showAlert('User not found!', 'danger');
                return;
            }
            
            try {
                await database.ref('users/' + userId).transaction((userData) => {
                    if (userData) {
                        userData.balance = (userData.balance || 0) + amount;
                        userData.totalEarned = (userData.totalEarned || 0) + amount;
                    }
                    return userData;
                });
                
                closeModal('addBalanceModal');
                showAlert(`Successfully added ${amount} points to user ${userId}!`, 'success');
                document.getElementById('addBalanceForm').reset();
            } catch (error) {
                console.error('Error adding balance:', error);
                showAlert('Failed to add balance.', 'danger');
            }
        });

        document.getElementById('changeBalanceForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const userId = document.getElementById('changeBalanceUserId').value;
            const newBalance = parseInt(document.getElementById('changeBalanceAmount').value);
            const newTonBalance = parseFloat(document.getElementById('changeTonBalanceAmount').value);
            
            if (!allUsers[userId]) {
                showAlert('User not found!', 'danger');
                return;
            }
            
            try {
                await database.ref('users/' + userId).update({
                    balance: newBalance,
                    tonBalance: newTonBalance
                });
                
                closeModal('changeBalanceModal');
                showAlert(`Successfully updated balances for user ${userId}!`, 'success');
                document.getElementById('changeBalanceForm').reset();
            } catch (error) {
                console.error('Error changing balance:', error);
                showAlert('Failed to change balance.', 'danger');
            }
        });

        document.getElementById('addTaskForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const isEditing = e.target.dataset.editingTaskId;
            const taskData = {
                title: document.getElementById('taskTitle').value,
                description: document.getElementById('taskDescription').value,
                url: document.getElementById('taskUrl').value,
                category: document.getElementById('taskCategory').value,
                telegramChannel: document.getElementById('taskTelegramChannel').value || null,
                reward: parseInt(document.getElementById('taskReward').value),
                impressions: parseInt(document.getElementById('taskImpressions').value),
                remainingImpressions: parseInt(document.getElementById('taskImpressions').value),
                status: 'active',
                createdBy: 'admin',
                createdAt: Date.now()
            };
            
            try {
                if (isEditing) {
                    // Keep existing impressions data when editing
                    const existingTask = allTasks[isEditing];
                    taskData.impressions = existingTask.impressions;
                    taskData.remainingImpressions = existingTask.remainingImpressions;
                    taskData.createdAt = existingTask.createdAt;
                    
                    await database.ref('tasks/' + isEditing).update(taskData);
                    showAlert('Task updated successfully!', 'success');
                } else {
                    await database.ref('tasks').push(taskData);
                    showAlert('Task created successfully!', 'success');
                }
                
                closeModal('addTaskModal');
                document.getElementById('addTaskForm').reset();
                
                // Reset form for next use
                delete document.getElementById('addTaskForm').dataset.editingTaskId;
                document.querySelector('#addTaskModal .modal-title').textContent = 'Create New Task';
                document.querySelector('#addTaskForm button[type="submit"]').innerHTML = '<span class="material-icons">add</span> Create Task';
                
            } catch (error) {
                console.error('Error with task:', error);
                showAlert('Failed to save task.', 'danger');
            }
        });

        document.getElementById('addCodeForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const code = document.getElementById('luckyCode').value.toUpperCase();
            const reward = parseInt(document.getElementById('luckyCodeReward').value);
            const expiryHours = parseInt(document.getElementById('luckyCodeExpiry').value);
            
            const codeData = {
                code: code,
                reward: reward,
                used: false,
                expiresAt: Date.now() + (expiryHours * 60 * 60 * 1000),
                createdAt: Date.now()
            };
            
            try {
                await database.ref('luckyCodes').push(codeData);
                closeModal('addCodeModal');
                showAlert(`Lucky code "${code}" created successfully!`, 'success');
                document.getElementById('addCodeForm').reset();
            } catch (error) {
                console.error('Error creating code:', error);
                showAlert('Failed to create lucky code.', 'danger');
            }
        });

        // Analytics
        function loadAnalytics() {
            const today = new Date().toDateString();
            
            // New users today
            const newUsersToday = Object.values(allUsers).filter(user => 
                user.createdAt && new Date(user.createdAt).toDateString() === today
            ).length;
            
            // Tasks created today
            const tasksCreatedToday = Object.values(allTasks).filter(task => 
                task.createdAt && new Date(task.createdAt).toDateString() === today
            ).length;
            
            // Withdrawals today
            const withdrawalsToday = Object.values(allWithdrawals).filter(withdrawal => 
                withdrawal.createdAt && new Date(withdrawal.createdAt).toDateString() === today
            ).length;
            
            // Banned users
            const bannedUsers = Object.values(allUsers).filter(user => user.status === 'banned').length;
            
            // Active tasks
            const activeTasksTotal = Object.values(allTasks).filter(task => task.status === 'active').length;
            
            // Total TON withdrawn
            const totalTonWithdrawn = Object.values(allWithdrawals)
                .filter(w => w.status === 'completed')
                .reduce((sum, w) => sum + (w.amountTon || 0), 0);

            document.getElementById('analyticsNewUsersToday').textContent = newUsersToday;
            document.getElementById('analyticsTasksCreatedToday').textContent = tasksCreatedToday;
            document.getElementById('analyticsWithdrawalsToday').textContent = withdrawalsToday;
            document.getElementById('analyticsBannedUsers').textContent = bannedUsers;
            document.getElementById('analyticsActiveTasksTotal').textContent = activeTasksTotal;
            document.getElementById('analyticsTotalTonWithdrawn').textContent = totalTonWithdrawn.toFixed(8);
            
            // IP Analysis
            loadIPAnalysis();
        }

        function loadIPAnalysis() {
            const ipStats = {};
            
            Object.values(allUsers).forEach(user => {
                if (user.ipAddress) {
                    if (!ipStats[user.ipAddress]) {
                        ipStats[user.ipAddress] = {
                            userCount: 0,
                            totalEarnings: 0
                        };
                    }
                    ipStats[user.ipAddress].userCount++;
                    ipStats[user.ipAddress].totalEarnings += (user.totalEarned || 0);
                }
            });
            
            const tbody = document.getElementById('ipAnalysisTableBody');
            tbody.innerHTML = '';
            
            Object.keys(ipStats)
                .sort((a, b) => ipStats[b].userCount - ipStats[a].userCount)
                .forEach(ip => {
                    const stats = ipStats[ip];
                    const row = document.createElement('tr');
                    
                    row.innerHTML = `
                        <td>${ip}</td>
                        <td>${stats.userCount}</td>
                        <td>${formatNumber(stats.totalEarnings)}</td>
                        <td>
                            <button class="btn btn-danger" onclick="banAllUsersByIP('${ip}')">
                                <span class="material-icons">block</span>
                                Ban All
                            </button>
                        </td>
                    `;
                    tbody.appendChild(row);
                });
        }

        async function banAllUsersByIP(ipAddress) {
            const usersToban = Object.keys(allUsers).filter(userId => 
                allUsers[userId].ipAddress === ipAddress && allUsers[userId].status !== 'banned'
            );

            if (usersToban.length === 0) {
                showAlert('No active users found with this IP address.', 'warning');
                return;
            }

            if (confirm(`This will ban ${usersToban.length} users with IP ${ipAddress}. Continue?`)) {
                try {
                    const updates = {};
                    usersToban.forEach(userId => {
                        updates[`users/${userId}/status`] = 'banned';
                    });
                    
                    await database.ref().update(updates);
                    showAlert(`Successfully banned ${usersToban.length} users with IP ${ipAddress}!`, 'success');
                    loadIPAnalysis(); // Refresh the IP analysis
                } catch (error) {
                    console.error('Error bulk banning users:', error);
                    showAlert('Failed to ban users. Please try again.', 'danger');
                }
            }
        }

        // System Settings Functions
        async function updateGameSettings() {
            const settings = {
                maxDailyAds: parseInt(document.getElementById('maxDailyAds').value),
                adRewardPoints: parseInt(document.getElementById('adRewardPoints').value),
                checkinReward: parseInt(document.getElementById('checkinReward').value)
            };
            
            try {
                await database.ref('systemSettings/game').update(settings);
                showAlert('Game settings updated successfully!', 'success');
            } catch (error) {
                console.error('Error updating game settings:', error);
                showAlert('Failed to update game settings.', 'danger');
            }
        }

        async function updateExchangeRates() {
            const rates = {
                pointsToTonRate: parseFloat(document.getElementById('pointsToTonRate').value),
                impressionsToTonRate: parseFloat(document.getElementById('impressionsToTonRate').value),
                referralRate: parseInt(document.getElementById('referralRate').value)
            };
            
            try {
                await database.ref('systemSettings/rates').update(rates);
                showAlert('Exchange rates updated successfully!', 'success');
            } catch (error) {
                console.error('Error updating exchange rates:', error);
                showAlert('Failed to update exchange rates.', 'danger');
            }
        }

        async function resetAllUserBalances() {
            if (confirm('Are you sure you want to reset ALL user balances to 0? This action cannot be undone!')) {
                if (confirm('This will reset balances for ALL users. Type "RESET" to confirm:') && 
                    prompt('Type RESET to confirm:') === 'RESET') {
                    try {
                        const updates = {};
                        Object.keys(allUsers).forEach(userId => {
                            updates[`users/${userId}/balance`] = 0;
                            updates[`users/${userId}/tonBalance`] = 0;
                        });
                        
                        await database.ref().update(updates);
                        showAlert('All user balances have been reset to 0!', 'success');
                    } catch (error) {
                        console.error('Error resetting balances:', error);
                        showAlert('Failed to reset balances.', 'danger');
                    }
                }
            }
        }

        // Utility Functions
        function filterUsers() {
            const searchTerm = document.getElementById('userSearch').value.toLowerCase();
            const rows = document.querySelectorAll('#usersTableBody tr');
            
            rows.forEach(row => {
                const text = row.textContent.toLowerCase();
                row.style.display = text.includes(searchTerm) ? '' : 'none';
            });
        }

        function confirmAction(action, description) {
            if (confirm(`Are you sure you want to ${description}? This action cannot be undone!`)) {
                if (action === 'clearAllTasks') {
                    clearAllTasks();
                } else if (action === 'clearAllWithdrawals') {
                    clearAllWithdrawals();
                }
            }
        }

        async function clearAllTasks() {
            try {
                await database.ref('tasks').remove();
                showAlert('All tasks cleared successfully!', 'success');
            } catch (error) {
                console.error('Error clearing tasks:', error);
                showAlert('Failed to clear tasks.', 'danger');
            }
        }

        async function clearAllWithdrawals() {
            try {
                await database.ref('withdrawals').remove();
                showAlert('All withdrawals cleared successfully!', 'success');
            } catch (error) {
                console.error('Error clearing withdrawals:', error);
                showAlert('Failed to clear withdrawals.', 'danger');
            }
        }

        function refreshData() {
            location.reload();
        }

        // Modal Management
        function showModal(modalId) {
            document.getElementById(modalId).style.display = 'flex';
        }

        function closeModal(modalId) {
            document.getElementById(modalId).style.display = 'none';
        }

        // Alert System
        function showAlert(message, type) {
            const alertContainer = document.querySelector('.container');
            const alert = document.createElement('div');
            alert.className = `alert alert-${type}`;
            alert.innerHTML = `
                <span class="material-icons">${type === 'success' ? 'check_circle' : type === 'warning' ? 'warning' : type === 'info' ? 'info' : 'error'}</span>
                ${message}
            `;
            alertContainer.prepend(alert);

            setTimeout(() => {
                if (alert && alert.parentNode) {
                    alert.remove();
                }
            }, 5000);
        }

        // Format Numbers
        function formatNumber(num) {
            if (num === null || num === undefined) return '0';
            num = Number(num);
            
            if (num >= 1e12) return (num / 1e12).toFixed(1) + 'T';
            if (num >= 1e9) return (num / 1e9).toFixed(1) + 'B';
            if (num >= 1e6) return (num / 1e6).toFixed(1) + 'M';
            if (num >= 1e3) return (num / 1e3).toFixed(1) + 'K';
            
            return num.toString();
        }

        // Close modal when clicking outside
        window.addEventListener('click', (e) => {
            if (e.target.classList.contains('modal')) {
                e.target.style.display = 'none';
            }
        });
    </script>
</body>
</html>
